# Front-end for cobashop application

This application is used to fullfill Pemrograman Web course.

## How to use

1. Run `yarn install` command
2. Run `yarn build` and `yarn start` command or
3. Run `yarn dev` for development mode
